import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    width: 120,
    paddingVertical: 10,
    elevation: 5,
    borderRadius: 2,
  },
  text: {
    justifyContent: 'center',
    alignItems: 'center',
    fontSize: 16,
    fontWeight: '400',
    color: '#ffffff',
  },
});
